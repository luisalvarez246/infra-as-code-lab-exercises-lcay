## Terraform Functions:

### element()
Select the elements of a list, with wraparound built-in functionality:

e.g. element(["a", "b", "c"], 6) returns "a"

### investigate

account management commands:
assume
granted

### steps to install (investigate first)
brew tap common-fate/granted
brew install granted

brew update
brew search assume
brew install assume